<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_main_menu"
        android:icon="@mipmap/ic_main_menu"
        android:title="@string/main_menu_icon"
        app:showAsAction="ifRoom"  />

    <item android:id="@+id/action_ONU"
        android:icon="@mipmap/ic_onu"
        android:title="@string/ONU_icon"
        app:showAsAction="ifRoom"  />

    <item android:id="@+id/action_new_account"
        android:icon="@mipmap/ic_new_account"
        android:title="@string/new_account_icon"
        app:showAsAction="ifRoom"  />

</menu>
